# Model benchmark

> **Status: BUILD COMPLETE — REAL RUN PENDING (API budget).**
> The benchmark toolchain is done and verified offline (dry run + unit tests).
> Numbers below arrive when the paid run happens; nothing here is guessed.

## Candidates

| Slot | Candidate | Path | License | Price |
|---|---|---|---|---|
| 1 | FASHN v1.6 (`fal-ai/fashn/tryon/v1.6`) | fal.ai API | Commercial (API terms) | $0.075/gen |
| 2 | FLUX VTO (`fal-ai/flux-pro/v1/vto`) | fal.ai API | Commercial (API terms) | ~$0.0475/gen (per-MP) |
| 3 | FASHN v1.5 | Self-host (Phase 6) | Apache-2.0 | GPU-hrs on Modal |

Request schemas verified 2026-09-01 from the official fal.ai API pages
(field-level detail lives in the adapter module docstrings).

## Method

- Same test set for every candidate: `test/person_*.jpg` × `test/garments/*`
- ~25 generations per candidate (~75–100 total, inside free credits)
- Machine metrics from `results.jsonl`: success rate, mean/P95 latency, est. cost
- Human metrics from `scores.csv` (1–10): identity preservation, garment
  fidelity, visual quality

## Results — PENDING

The table below is generated by:

```bash
uv run python -m ai.benchmark.run_benchmark run -c fashn_v1_6 -c flux_vto
# fill output/benchmark/scores.csv (1-10), then:
uv run python -m ai.benchmark.build_report
```

| Candidate | Runs | Success | Mean s | P95 s | Est. cost $ | Identity | Garment | Visual | Overall |
|---|---|---|---|---|---|---|---|---|---|
| fashn_v1_6 | — | — | — | — | — | — | — | — | — |
| flux_vto | — | — | — | — | — | — | — | — | — |

## Decision — PENDING

- [ ] One API model picked for the demo
- [ ] One self-host model picked for Phase 6 (default: FASHN v1.5, Apache-2.0)
- [ ] Licenses verified on official pages; MODEL_LICENSES.md updated

## Verified so far (offline, zero spend)

- Adapter request schemas match the official docs (unit-tested field-by-field)
- Full benchmark loop proven in dry-run mode: discovery → validation →
  generation calls (fake) → records → scores template → report builder
- Offline gate: ruff (ALL) clean, basedpyright 0 errors, 24/24 tests green
